function best_airfoil = Airfopt(Cl, GuessAirfoil)
%Read data from naca A BCC
    % Extract Cl/Cd for input Cl = y1
%Read data from naca A+1 BCC
    % Extract Cl/Cd for input Cl = y2
%derivative = y1 - y2 / 1

% if derivative > 0and A < 9, 
    %increase A 
%if derivative < 0 and A > 1, 
    %decrease A and A > 1
end
